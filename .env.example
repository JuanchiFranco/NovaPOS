# Usado solo por el CLI de Prisma (prisma generate / prisma migrate dev) en desarrollo.
# En tiempo de ejecución, la app (src/main/database/client.ts) fija esta variable
# dinámicamente según el entorno (dev vs. producción empaquetada).
DATABASE_URL="file:./prisma/dev.db"
